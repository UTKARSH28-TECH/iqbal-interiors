/**
 * Curated categories to feature, in order (the first is the large tile).
 *
 * Only the *selection* lives here — cover images are read dynamically from the
 * gallery folders at build time, so no image paths are hardcoded. Slugs that no
 * longer exist as folders are skipped automatically.
 */
export const featuredCategorySlugs = [
  "bedroom",
  "false-ceiling",
  "tv-unit-design",
  "hall",
  "kitchen",
];
